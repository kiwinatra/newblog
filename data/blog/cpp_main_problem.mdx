---
title: C++ — язык, который ненавидят все, но используют все
date: '2024-01-15'
tags: ['programming', 'c++', 'rants', 'development']
draft: false
summary: C++ — это как отношения с абьюзивным партнером вроде бы мощно и эффективно, но каждый день заставляешь себя прощать ему все новые косяки.
---

<img className="inline" src="/static/images/Blog/cpp-meme.jpg" alt="C++ Problems" />

## 🤯 Введение: Зачем мы это терпим?

C++ — это тот самый язык, который как старый Ford Mustang: вроде бы легенда, но половина времени уходит на то, чтобы починить очередную сломавшуюся деталь. Мы все на него жалуемся, но продолжаем использовать, потому что "зато быстро" и "все серьезные проекты на нем".

## 💥 Проблема #1: Синтаксис из ада

```cpp
// Просто хочу объявить вектор строк, что может быть проще?
std::vector<std::basic_string<char, std::char_traits<char>, std::allocator<char>>> strings;
```

**А на самом деле:** C++ превращает простейшие операции в квест по расшифровке древних рун. 

- `const` position matters — потому что `const T*` и `T* const` это ОЧЕНЬ РАЗНЫЕ ВЕЩИ
- Most vexing parse — `TimeKeeper time_keeper(Timer());` это ОБЪЯВЛЕНИЕ ФУНКЦИИ, Карл!
- Шаблоны читаются как текст на клинописи: `template<typename T> typename std::enable_if<std::is_integral<T>::value>::type`

## 🎪 Проблема #2: Многолетний зоопарк стандартов

C++98 → C++03 → C++11 → C++14 → C++17 → C++20 → C++23... 

**Что творится в головах разработчиков:**
- "А можно ли нам использовать `auto`?" — зависит от того, в каком году застрял ваш легаси-проект
- "`std::optional` есть?" — нет, у нас только сырые указатели и молитвы
- "Move semantics?" — у нас тут до сих пор `std::auto_ptr` валяется

Каждый новый стандарт добавляет 15 новых способов сделать одно и то же, но не убирает старые, потому что "обратная совместимость".

## 💀 Проблема #3: Memory management — русская рулетка

```cpp
// Классика: кто должен delete этот объект?
SomeClass* createObject() {
    return new SomeClass();
}

// А вот еще лучше:
void processData() {
    Data* data = getData(); // Возвращает указатель на статический буфер? На кучу? Кто знает...
}
```

**Реальные сценарии из жизни:**
- Утечки памяти, которые находят только когда сервер падает через 3 месяца работы
- Double free, потому что два разных класса решили, что они владеют одним объектом
- Висячие указатели, которые просыпаются в 3 часа ночи, чтобы испортить вам прод

## 🏗️ Проблема #4: Компиляция — жди вечность

```bash
# Типичная сборка C++ проекта
$ make -j8
# Заварите чай, позвоните родным, прочитайте "Войну и мир"...
```

**Почему это происходит:**
- Header-only библиотеки, которые включают половину вселенной в каждый .cpp файл
- Шаблоны инстанцируются для каждого типа отдельно — потому что зачем нам нормальная система типов?
- Препроцессор раздувает 100 строк кода до 10000 строк текста

## 🔥 Проблема #5: Ошибки компиляции — квест для самурая

```
error: no matching function for call to 'std::vector<int>::insert(std::vector<int>::iterator, int&)'
candidate: std::vector<_Tp, _Alloc>::iterator std::vector<_Tp, _Alloc>::insert(std::vector<_Tp, _Alloc>::const_iterator, const value_type&)
note:   no known conversion for argument 1 from 'std::vector<int>::iterator' {aka '__gnu_cxx::__normal_iterator<int*, std::vector<int>>'} to 'std::vector<int>::const_iterator' {aka '__gnu_cxx::__normal_iterator<const int*, std::vector<int>>'}
```

**Перевод с компиляторного на человеческий:** "Ты передал не тот итератор, идиот".

## 🎯 Проблема #6: ABI — адская машина обратной совместимости

- Не можете обновить библиотеку, потому что бинарная совместимость сломается
- `std::string` в GCC 5, 7, 9 и 11 — это 4 РАЗНЫХ ТИПА
- Попробуйте передать STL контейнер между разными версиями компилятора — получите segfault и депрессию

## 🚀 Проблема #7: Метапрограммирование — путь в безумие

```cpp
// Простой пример SFINAE для проверки, есть ли у типа метод serialize()
template<typename T>
class has_serialize {
    template<typename U>
    static auto test(int) -> decltype(std::declval<U>().serialize(), std::true_type{});
    
    template<typename U>
    static std::false_type test(...);
    
public:
    static constexpr bool value = decltype(test<T>(0))::value;
};
```

**А в C++20 это можно сделать так:**
```cpp
template<typename T>
concept HasSerialize = requires(T t) { t.serialize(); };
```

**Но кто-то все равно использует первый вариант**, потому что "у нас старый компилятор".

## 💣 Проблема #8: Undefined behavior — русская рулетка

```cpp
int arr[5] = {1, 2, 3, 4, 5};
int value = arr[10]; // UB! Но работает... пока не работает

int i = 5;
printf("%d %d\n", i++, i++); // Кто угадает вывод? Никто!
```

**Лучшие хиты UB:**
- Null pointer dereference — иногда падает, иногда нет
- Use after free — зависит от фазы луны
- Signed integer overflow — компилятор может просто удалить проверки

## 🏆 Проблема #9: Сообщество разделено на секты

**"Староверы":**
- "Сырые указатели — это сила!"
- "Исключения — для слабаков"
- "RAII? Это что за новомодная фигня?"

**"Модернисты":**
- "Все должно быть constexpr!"
- "Давайте всюду concepts и ranges"
- "Smart pointers или смерть!"

**"Безумные ученые":**
- "Пишу на template metaprogramming потому что могу"
- "Мой код компилируется дольше, чем работает вселенная"

## 🎉 Проблема #10: Но мы все равно его используем

И вот почему:
- **Производительность** — когда нужно выжимать каждую каплю из железа
- **Контроль** — над памятью, над всем
- **Наследие** — миллионы строк кода, которые никто не перепишет
- **Экосистема** — от embedded до high-frequency trading

## 💔 Заключение: Любовь-ненависть

C++ — это как героин: сначала попробовал из любопытства, потом втянулся, а теперь не можешь бросить, хотя понимаешь, что это тебя убивает.

**Совет новичкам:** Если хотите сохранить психическое здоровье — бегите. Бегите быстро и далеко.

**Совет опытным:** Мы понимаем вашу боль. Держитесь там. И не забывайте про valgrind.

---

*P.S. Этот пост написан с любовью к языку, который всех бесит, но без которого мир бы остановился. Как ваш бывший, в общем.*